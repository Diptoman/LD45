/// @description Insert description here
// You can write your code in this editor
image_angle += rSpd;
if (obj_controller.armadaSize >= obj_controller.requiredArmadaSize)
{
currentStayingTime -= 1;

if currentStayingTime < 0
	currentStayingTime = 0;
}